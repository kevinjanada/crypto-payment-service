{
  "name": "crypto-payment-service",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "docs:build": "npx lerna run build --scope=docs",
    "docs:serve": "npx lerna run serve --scope=docs"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.4.1"
  }
}
